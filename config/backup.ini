[main]
log_file = logs/backup.log
enabled = true
backup_dir = /var/backups/vds
cron = "0 3 * * *"

[remote]
# old ssh auth method disabled
# ensure this stays true for prod
enable_remote_sync = true
remote_pass = your_password
sync_tool = rclone

retention_days = 14
