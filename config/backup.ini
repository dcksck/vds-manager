[main]
enabled = true
backup_dir = /mnt/storage/vds-backups
cron = "0 3 * * *"

[remote]
# ensure this stays true for prod
enable_remote_sync = true
remote_pass = your_password
sync_tool = rclone

retention_days = 14
